# 手动测试指南:自己拼提示、自己看编号、自己跑模型

这份文档写给"想亲手改提示、亲眼确认模型收到了什么"的人。它回答三个问题:有哪几条路径能把内容送进模型、每条路径上你怎么看见真实输入、以及在哪里最容易踩坑。

---

## 一、先分清三条路径

**第一条:模板路径。** 你把"谁说了什么"的消息清单交给模型的对话模板,由模板拼成整段文本。这是真实部署走的路。对应代码是 `tokenizer.apply_chat_template(...)`。

**第二条:原始文本路径。** 你自己把整段文本写好(包括 `<|im_start|>`、`<|im_end|>` 这些标记),跳过模板,直接把它编成编号喂给模型。这是本实验里"伪装"类攻击必须走的路——因为要伪造的正是模板才会写的那部分。

**第三条:对话界面路径。** 图形界面的聊天软件、或者命令行里的对话模式。**这条路上你给的内容会被再套一层模板**,因为那些程序默认按"对话"来处理你的输入。

第三条是最大的坑,而且它不会报错。我在本机就踩过一次:把整段写好的提示用 `llama-cli` 的 `-f` 参数喂进去,程序接受了、也照常生成了,但它在我的提示外面又套了一层对话模板——模型收到的其实是"把那一整段当成一条用户消息"再包一层的结果,和我写的完全不同。同一条提示换成不套模板的入口之后,模型的输出立刻变了。所以:

> **要研究"模型到底收到了什么",只能用第一条或第二条路径,并且必须能在跑之前把编号打出来核对。**

---

## 二、本仓库现成的工具(不用装任何东西)

`tools/manual_probe.py` 把前两条路径都做好了,而且跑之前会先把编号打给你看:

```bash
# 看渲染器本身:打印该模型的对话模板源码(Jinja)
python tools/manual_probe.py --template

# 看"正常一轮对话"渲染成什么:整段文本 + 逐段编号,标出轮次开始符/结束符
python tools/manual_probe.py --render "你好"

# 自己手写整段提示直接喂进去(只经过分词,不套模板)
python tools/manual_probe.py --raw-file 我的提示.txt

# 走官方模板问一句,作为对照
python tools/manual_probe.py --chat "你好"
```

`--render` 只加载分词器,几秒钟;`--raw-*` 与 `--chat` 会加载权重。

手写文件里照模板的写法打,**开始符和结束符都要写全**:

```
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
你好<|im_end|>
<|im_start|>assistant
```

工具会先把你写的这段编成编号打印出来。**判据很简单:你写的 `<|im_start|>` 应该被编成一个编号(本模型是 248045),而不是被切成好几个普通编号。** 如果被切碎了,说明写法与该模型的分隔符不完全一致,后面测什么都没意义。

---

## 三、用 llama.cpp 手动跑

llama.cpp 的官方发布页有各平台的预编译包(Windows、Linux、macOS,含 CUDA 与 Vulkan 版本)。解压即用,里面有一组小程序,名字容易混,这里说清楚各自是干什么的:

| 程序 | 用途 | 会不会套模板 |
|---|---|---|
| `llama-tokenize` | **只看编号**,不跑模型生成 | 不套,给你原始编号 |
| `llama-server` | 起一个本地服务,提供接口 | **`/completion` 接口不套**;`/v1/chat/completions` 会套 |
| `llama-cli` | 命令行对话 | **会套**(默认对话模式) |
| `llama-completion` | 命令行续写 | 会额外加一段固定内容(实测多出 8 个片段) |

### 3.1 看编号

```bash
llama-tokenize -m 模型.gguf -f 我的提示.txt --ids --no-bos
```

`--ids` 直接打印编号列表;`--no-bos` 表示不要自动加起始符,和 Hugging Face 那边 `add_special_tokens=False` 的算法对齐。**把这条命令的输出和 Hugging Face 分词器的输出逐位比对,是判断一个引擎可不可信的最直接办法。**

### 3.2 跑模型(唯一能保证"你写什么就喂什么"的命令行入口)

```bash
# 起服务(上下文和批大小都开小一点,4GB 显存开大会分配失败)
llama-server -m 模型.gguf -ngl 99 -c 512 -b 256 -ub 256 --port 18080

# 另开一个终端,把手写的整段提示发过去
curl -s http://127.0.0.1:18080/completion \
  -H 'Content-Type: application/json' \
  -d "$(python3 -c "import json;print(json.dumps({'prompt':open('我的提示.txt',encoding='utf-8').read(),'n_predict':128,'temperature':0}))")"
```

返回值里有两项直接回答"它到底收到了什么":

- `tokens_evaluated`:服务端实际处理的提示片段数。**拿它和你手写内容的片段数比,对得上才说明没有附加内容。**
- `timings.predicted_per_second`:逐段生成速度。

### 3.3 本机实测对照(同一条手写提示,109 个片段)

| 引擎与档位 | 提示处理 | 逐段生成 | 生成内容 |
|---|---|---|---|
| llama.cpp(BF16,走 Vulkan) | ~2200 片段/秒 | **92.0 片段/秒** | `BLOCKED` |
| 本仓库(transformers,不压缩) | ~1400 片段/秒 | **45.1 片段/秒** | `BLOCKED` |
| llama.cpp(Q4_0,四比特) | ~2250 片段/秒 | **161.3 片段/秒** | `BLOCKED` |
| 本仓库(transformers + 四位压缩) | 1416 片段/秒 | **38.2 片段/秒** | `BLOCKED` |

三条结论:

1. **同精度下 llama.cpp 约快一倍,四比特下约快三到四倍。** 差距全在逐段生成这一段;提示处理两边基本持平,因为那是一次性的大矩阵运算,都能把显卡吃满,而逐段生成受显存带宽限制,llama.cpp 按量化格式直接算,不必每步先把权重量化回高精度。
2. **编号完全一致。** 同一条提示,Hugging Face 分词器与 llama.cpp 给出的 109 个编号逐位相同,包括手写的 `<|im_start|>`(248045)与 `<|im_end|>`(248046)。llama.cpp 不会把你写的标记"消毒"掉。
3. **输出一致。** 同精度、同提示,两边都给出 `BLOCKED`。

上面这张表不是手抄的,可以用仓库里的脚本复现:

```bash
# 只比对编号(不加载权重,几秒钟)
python tools/compare_llamacpp.py --gguf ~/models/gguf/Qwen3.5-0.8B-Q4_0.gguf

# 先在另一个终端起服务,再加 --port 测速度并核对 tokens_evaluated
llama-server -m 模型.gguf -ngl 99 -c 512 -b 256 -ub 256 --port 18080
python tools/compare_llamacpp.py --gguf 模型.gguf --port 18080

# 换成自己写的提示
python tools/compare_llamacpp.py --gguf 模型.gguf --prompt-file 我的提示.txt
```

脚本会把同一条提示分别送进两边的分词器,逐位比对编号,并在测速度时打印服务端报告的 `tokens_evaluated`——它必须等于你手写内容的片段数。

---

## 四、在 Windows 上跑:注意事项

**1. 优先选 CUDA 版而不是 Vulkan 版。** 发布页上有 Windows 的 CUDA 预编译包(`llama-*-bin-win-cuda-*.zip`)。Windows 上 CUDA 版通常比 Vulkan 版快。如果提示缺少运行库,再下载同一发布页上的 `cudart-llama-bin-win-cuda-*.zip` 解压到同一目录。版本号要对上(提示里的 CUDA 版本要和包名一致)。

**2. 换行符会改变分词结果,这是最容易中招的一条。** 用记事本保存的文本默认是 CRLF(每行结尾多一个回车符)。多出来的这个字符会变成额外的片段,你手写的整段输入就和你以为的不一样了。**办法:保存时选 UTF-8 且换行用 LF;或者保存后立刻用 `llama-tokenize --ids` 数一遍片段数,和预期不符就是这里出了问题。**

**3. 编码用 UTF-8,不要带 BOM。** 带 BOM 的文件开头会多出几个隐藏字节,同样会改变输入。

**4. 别用命令行直接传长文本。** Windows 下 `cmd` 和 PowerShell 的引号规则与 Linux 不同,长文本里又有引号又有换行,极易被截断或转义错。**一律把提示写进文件,用 `-f` 或从文件读取后发请求。**

**5. 显存比 Linux 更紧张。** Windows 桌面本身常占 1GB 以上显存。上下文和批大小都开小:`-c 512 -b 256 -ub 256`。开大会直接报 `failed to allocate Vulkan0 buffer`(本机在 Linux 上开 `-c 2048` 就撞过这个)。

**6. 端口与防火墙。** `llama-server` 默认只监听本机。如果要从别的机器访问,注意 Windows 防火墙会拦;测试阶段建议就监听 127.0.0.1。

**7. 一定核对 `tokens_evaluated`。** 这是你在 Windows 上唯一能确认"喂进去的就是我写的"的手段,每换一条提示都值得看一眼。

---

## 五、最容易踩的坑,按严重程度排

1. **跑了对话模式却以为在跑原始文本。** 表现:程序正常出结果,但结果解释不通。判据:`tokens_evaluated` 和你手写内容的片段数对不上。
2. **文件换行符或编码不对。** 表现:片段数差几个,肉眼看不出来。判据:同上。
3. **引擎偷偷清洗特殊标记。** 有些聊天软件会在送进模型前把 `<|im_start|>` 这类字符串转义成普通文字,使攻击根本到不了模型。判据:`llama-tokenize --ids` 看那几个标记是不是各自一个编号(248045/248046),而不是被切碎。
4. **量化档位不同却直接比较。** 同一个提示,不压缩与四比特压缩下模型的输出形态可能完全不同(本实验里出现过"只回七个片段就停"与"写满一长串代码块"两种)。要比较就必须同档位。
5. **上下文开太大导致显存分配失败。** 报错信息形如 `failed to allocate ... buffer` / `ErrorOutOfDeviceMemory`。把 `-c`、`-b`、`-ub` 调小。

---

## 六、回到本实验

本仓库的 `tools/manual_probe.py` 覆盖了本文第一、二节的全部功能,并且用的是官方模板那条路径(和真实部署一致)。`llama.cpp` 的价值在于快:日常手动试提示用它,速度是这条路径的三到四倍;但每换一条新提示,建议先用 `llama-tokenize --ids` 和 Hugging Face 分词器对一次编号,确认无误后再大量跑。
