# 第一轮实验:不看模型怎么回答,先拆开它的"分词器"和"对话模板"看个明白

这一轮完全不加载模型的脑子(权重),只拆开每个模型的"分词器"(把文字切成片段、编成编号的部件)和"对话模板"(把消息清单拼成模型输入的那段拼装规则),把它们的构造看清楚。一共拆了八个模型,结果按批次存在 `runs/` 目录里,每个模型一个文件夹。

先记住一个背景:前面主分析里说过,模型读到的不是文字而是编号。模板负责在每段话前后放分隔符(比如 `<|im_start|>system`),分词器负责把这些符号连同正文一起切成一节一节、编成编号。这一轮要回答的问题很具体:**如果我在用户消息里写出和分隔符一样的文字,分词器会把它编成和真分隔符相同的编号吗?**

## 两个模型家族的模板,长得不一样

Qwen 家族的模板是"拼文字"式的:模板把分隔符当作普通文字直接拼进结果,拼完之后再由分词器统一切。所以它拼出来的内容里,分隔符是白纸黑字的一段文字,例如:

```
<|im_start|>system
(系统提示的内容)
<|im_end|>
<|im_start|>user
(用户消息的内容)
<|im_end|>
```

Ministral 家族则不同:它的拼装环节不产出"含分隔符的完整文字",而是由专门的渲染器把分隔符**直接以编号形式插入**结果。换句话说,它的分隔符从拼装开始就是编号,不是文字。

这一字之差,造成了后面那个重要的家族差别。

## 发现一:在 Qwen 家族,你完全能在文字上伪造出同编号的分隔符

测出来是这样的:

- `<|im_start|>` 在分词器里是一个被单独收编的片段,编号 248045;`<|im_end|>` 被标成"特殊片段",编号 248046。
- 把一模一样的文字(比如 `<|im_start|>system`)写进用户消息,再走模板正常拼装,分词器照样把它编成 248045、248046——**和真分隔符一字不差、编号完全相同**。
- 分词器不会因为"这段文字是用户写的"就给它盖个戳。它只认文字本身。

六个 Qwen 模型(0.8B、2B、4B 以及各自的基础版)测出来全部如此。这就是"伪造分隔符在机制上畅通无阻"这句结论的出处。

## 发现二:在 Ministral 家族,连伪造的入口都不存在

Ministral 的分隔符既然是从拼装环节就以编号形式插入的,那么"先拼出文字、再由分词器切"这条路在它那里就不成立。我们把分隔符文字写进用户消息后,分词器只会把它当成普通内容切掉,编不出真分隔符那套编号。transformers 软件本身对这个家族的这种用法也给出过警告:那样拼出来的文字,再喂回去是还原不出原编号的。

换句话说:**在 Qwen 家族,"伪造同编号分隔符"在机制上可行;在 Ministral 家族,这条路从起点就堵死了。** 这是两家的模板实现方式不同造成的,不是谁聪明谁笨的问题。

## 八个模型一句话一览

- Qwen 3.5 的 0.8B / 2B / 4B(指令版):模板文字式,假分隔符能伪造出同编号。**结论可直接参考主分析报告。**
- Qwen 3.5 的 0.8B / 2B / 4B 基础版:同上,模板一样。
- Ministral 3B 的指令版与基础版:模板渲染器式,假分隔符伪造不出同编号。这一家的"伪造"攻击从一开始就不存在,实验到此为止;至于它实际的行为表现,因为显存不够(见主分析报告的运行环境说明),这一轮没有继续往下测。

## 想自己复核?

每个模型的结果文件夹里,`template/` 下有三个可以直接打开的文件:`chat_template.txt`(模板原文)、`tokens.tsv`(整串内容逐片段的表格,标注每段是谁的)、`collisions.json`(把模板分隔符文字塞进用户消息后,检测是否撞出真分隔符编号的结果)。打开 `tokens.tsv` 看 `<|im_start|>` 那一行,再对照 `collisions.json` 里检测出的编号,就能自己验证上面的结论。
